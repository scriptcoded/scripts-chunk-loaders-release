{
  "private": true,
  "description": "This file only contains dependencies for the release scripts and not the mod itself.",
  "devDependencies": {
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/github": "^11.0.3",
    "semantic-release": "^24.2.7"
  }
}
